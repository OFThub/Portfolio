# Canonical origin, used for og:url, the canonical link, sitemap.xml and
# robots.txt. On Vercel this is filled in automatically from the deployment's
# own hostname, so you only need to set it when a custom domain is attached.
#
# VITE_SITE_URL=https://your-domain.dev
